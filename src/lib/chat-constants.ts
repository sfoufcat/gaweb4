// Global chat channel IDs (safe for client-side use)
export const ANNOUNCEMENTS_CHANNEL_ID = 'announcements';
export const SOCIAL_CORNER_CHANNEL_ID = 'social-corner';
export const SHARE_WINS_CHANNEL_ID = 'share-wins';

// Note: generateCoachingChannelId is in chat-server.ts (server-only, uses Node crypto)
